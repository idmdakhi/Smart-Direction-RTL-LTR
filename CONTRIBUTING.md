# مشارکت در Smart Direction

با تشکر از علاقه‌ی شما به مشارکت در این پروژه! لطفاً پیش از ارسال درخواست، این راهنما را مطالعه کنید.

## نحوه‌ی مشارکت

1. **گزارش باگ**: از بخش Issues استفاده کنید و توضیح کامل همراه با مراحل بازتولید ارائه دهید.
2. **پیشنهاد قابلیت جدید**: یک Issue با برچسب `enhancement` باز کنید.
3. **Pull Request**:
   - شاخه‌ی `main` را fork کنید.
   - تغییرات خود را در یک شاخه‌ی جداگانه انجام دهید.
   - کد را با `eslint` و `prettier` یکپارچه کنید.
   - تست‌های مربوطه را اضافه کنید (در صورت وجود).
   - Pull Request را با توضیح کامل باز کنید.

## اصول کدنویسی

- برای توابع، JSDoc بنویسید.
- نام‌گذاری به‌صورت `camelCase` برای متغیرها و توابع، `PascalCase` برای کلاس‌ها.
- خطاها را با `try/catch` بگیرید و با `logError` ثبت کنید.
- از `utils.js` برای توابع عمومی استفاده کنید.

## محیط توسعه

برای اجرا در حالت توسعه:

1. پروژه را کلون کنید.
2. در کروم به `chrome://extensions` بروید.
3. حالت توسعه‌دهنده را فعال کنید.
4. روی «Load unpacked» کلیک کرده و پوشه را انتخاب کنید.

برای تغییر `DEBUG` در `utils.js` به `true` برای مشاهده لاگ‌ها.

## تست

- تست‌های واحد با `jest` در آینده اضافه خواهند شد.
- فعلاً تست دستی روی سایت‌های مختلف انجام دهید.

## سوالات؟

اگر سوالی دارید، یک Issue باز کنید یا با ایمیل تیم تماس بگیرید.

با سپاس از مشارکت شما 🙏
